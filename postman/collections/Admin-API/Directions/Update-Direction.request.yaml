$kind: http-request
method: PUT
url: '{{baseUrl}}/api/admin/directions/{{directionId}}'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "Обновлённое направление",
      "description": "Описание"
    }
